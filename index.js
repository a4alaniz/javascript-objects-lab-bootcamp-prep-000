function objects() {
  object[recipes] = objects
}

function updateObjectWithKeyAndValue(object, key, value) {
  return Object.assign({}, obj, {[key]: value})
  var obj = {prop: 1}
}


function deleteFromObjectByKey(object, key) {
  var obj = {prop: 1}
  var newObj = deleteFromObjectByKey(object, 'prop')
  newObj
  obj
}